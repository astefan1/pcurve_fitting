% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/heatmap.R
\name{heatmap}
\alias{heatmap}
\title{Heatmap of a Specified “rmse” Column}
\usage{
heatmap(sim, DV)
}
\arguments{
\item{sim}{A data frame containing at least the following columns:
\itemize{
\item \code{nvar}: predictor count (numeric or integer)
\item \code{r}: response level (numeric or factor)
\item \code{strategy}: grouping variable for faceting
\item one or more columns with names containing “rmse”
}}

\item{DV}{A string naming the column in \code{sim} to plot (must be one
of the “rmse” columns)}
}
\value{
A \code{ggplot} object showing the heatmap.
}
\description{
Creates a tile-based heatmap of the chosen dependent variable (\code{DV})
across predictors (\code{nvar}) and response levels (\code{r}), faceted by
\code{strategy}. The color scale is shared and fixed across all columns
whose names contain “rmse”, ensuring comparability.
}
\details{
The fill scale uses the “inferno” viridis palette (reversed) and
skews to emphasize the lowest 10\% of the RMSE distribution. The
limits of the scale are taken from the minimum and maximum across
all “rmse” columns in \code{sim}.
}
\examples{
\dontrun{
library(dplyr)
# simulate some data
simres <- expand.grid(
  nvar = 1:5,
  r = seq(0, 1, length.out = 5),
  strategy = c("A", "B")
) \%>\%
  mutate(
    rmseSotola = runif(n(), 0, 1),
    rmseOther  = runif(n(), 0, 2)
  )

# plot one of the rmse columns
heatmap(simres, "rmseSotola")
}
}
